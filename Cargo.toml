[package]
name = "risky"
description = "a minimal RISC-V kernel written in Rust"
version = "0.1.0"
edition = "2024"

[dependencies]
fdt-rs = { version = "0.4.5", default-features = false }
talc = "4.4.3"

# We have no tests/benchmarking yet
[[bin]]
name = "risky"
test = false
bench = false
